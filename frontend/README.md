# Zarr Frontend

Vue workspace for the file-management and geo-viewer apps.

## Apps

- `@apps/manage`: data download, raw file inspection, Zarr browsing
- `@apps/viewer`: WebGL geospatial viewer

Both apps use Vue 3, Vuex, Tailwind CSS, and `vite-ssg` for static generation.
